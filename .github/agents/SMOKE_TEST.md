# Reviewer smoke test

This file exists only to give the `vetretire-reviewer` bot a trivial, harmless diff to review as Phase 1 of the agent-team rollout (see the agent-team plan). It carries no functional meaning and can be deleted once the reviewer bot has confirmed it can comment/review a PR labeled `agent-pr` on an `agent/**` branch without editing any files.
