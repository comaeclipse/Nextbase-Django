# Admin panel removed 2026-07-07 per request (rarely used; to be redone later).
# The custom LocationAdmin/StateInfoAdmin, the spreadsheet editor, and the
# admin/ route (in vetretire_project/urls.py) were all removed. No models are
# registered with the Django admin.
